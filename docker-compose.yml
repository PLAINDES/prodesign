services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - DB_HOST=db
    container_name: prodesign-react
    ports:
      - "5199:80"
    restart: always
    networks:
      - prosupervision_network

networks:
  prosupervision_network:
    external: true